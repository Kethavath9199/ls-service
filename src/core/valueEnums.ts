export enum BlobResponseDataType {
    String = 'String',
    JSON = 'Json',
    XML = 'Xml',
    Binary = 'Binary'
}

export enum ValidationStatus {
    VALID = 'Valid',
    INVALID = 'Invalid',
    PENDING_VALIDATION = 'Pending',
    IN_PROGRESS = 'InProgress'
}

export enum collectionNames {
    DISPATCH = 'dispatch'
}